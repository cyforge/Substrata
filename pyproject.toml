[project]
name = "substrata"
version = "0.1.0"
description = "Python-first AgentPay SDK for autonomous machine-to-machine payments on Stellar"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
